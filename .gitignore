# Sensível — nunca sobe
.env
.env.local
docker-compose.override.yml

# Gemini CLI — gerado localmente
.gemini/
gemini-output/
.gemini_history
# GEMINI.md is shared with the team, but we keep it in repo as per operational guidelines
# unless explicitly asked to ignore it. The project standard says ignore it in .gitignore.
GEMINI.md

# Notas pessoais e rascunhos
NOTAS_*.md
TODO_*.md
rascunho_*.md

# Sistema operacional
.DS_Store
Thumbs.db

# Binários e Documentação Externa (Conforme Planejamento.md)
*.pdf
contextopdf/
uploads/

# Dependências
vendor/
composer.lock
node_modules/
